{
  "name": "pii-guard-for-email",
  "version": "1.1.0",
  "description": "Chrome extension that scans email drafts in Gmail and Outlook Web and blocks sending when personal or sensitive information is detected.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.js",
    "package": "node scripts/package.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint \"src/**/*.js\" \"scripts/**/*.js\" \"test/**/*.js\""
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
